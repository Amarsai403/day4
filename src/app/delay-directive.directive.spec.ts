import { DelayDirectiveDirective } from './delay-directive.directive';

describe('DelayDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
